<nav class="navbar navbar-default" role=navigation> <!--nav是一個有結束的標籤 問題 為什麼要+role?-->
<div class="container-fluid"> <!--bootstrap自設RWD-->
   

   <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
   
        <ul class="nav navbar-nav navbar-left">
          <li> <%= link_to("Products list", products_path) %> </li>
        </ul>
      
     <ul class="nav navbar-nav navbar-right">
      <li>
          <%= link_to carts_path do %>
          購物車<i class="fa fa-shopping-cart"> </i> ( <%= render_cart_items_count(current_cart) %> )
          <% end %>
      </li>
      <% if !current_user %>
         <li><%= link_to("註冊",)%>
         <li><%= link_to("登入", new_user_session_path) %>
      <% else %>
         <li class="dropdown">
         <a href="#" class="dropdown-toggle" data-toggle="dropdown">
         Hello! <%= current_user.email %>
         <b class="caret"></b></a>
         <ul class="dropdown-menu">
         <% if current_user.admin? %>
           <li><%= link_to("Admin", admin_products_path ) %></li>
           <li> <%= link_to("Users", admin_users_path) %> </li>
           <li> <%= link_to("All order", admin_orders_path) %></li>
         <% end %>
         <li><%= link_to("我的訂單", account_orders_path) %></li>
           <li><%= link_to("登出", destroy_user_session_path, method: :delete) %>
         </ui>
         </li>
      <% end %>
     </ul>

   </div>

</div>
</nav>